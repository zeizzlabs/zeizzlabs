# ZeizzLabs — asset brief

Everything the site can display, the exact path it reads, and a prompt to
generate it. **Filenames are the contract**: drop a file at the listed path and
it appears. No code change, no rebuild step beyond the normal one.

Placeholders exist for every image below (generated by
`python3 scripts/generate-art.py`), so nothing is broken while you work through
this. Replace them in any order.

---

## House style — put this in every image prompt

> Deep navy-black background (#04060C). Electric blue (#1E7BFF) and champagne
> gold (#DCB877) as the only accent colours, with brushed steel (#A4B3C9) for
> structure. Circuit-board motifs: orthogonal traces with rounded corners and
> small ring-shaped terminal pads. Premium, restrained, technical. Cinematic
> lighting, subtle film grain, no text, no logos, no watermarks, no people
> unless stated.

Avoid: purple, teal, neon pink, lens flares, stock-photo gloss, generic "AI
brain" imagery, floating holograms, cliché matrix code rain.

---

## 1. Service artwork — 8 images · 2400 × 1000 · JPG

Full-bleed header on each `/services/[slug]` page, and the artwork inside the
cursor preview card.

| Path | Pillar |
| --- | --- |
| `public/media/service-software-development.jpg` | Software & Development |
| `public/media/service-ai-intelligence.jpg` | AI & Intelligence |
| `public/media/service-automation-workflows.jpg` | Automation & Workflows |
| `public/media/service-design-branding.jpg` | Design & Branding |
| `public/media/service-digital-products.jpg` | Digital Products |
| `public/media/service-cloud-infrastructure.jpg` | Cloud & Infrastructure |
| `public/media/service-data-analytics.jpg` | Data & Analytics |
| `public/media/service-digital-experiences.jpg` | Digital Experiences |

**Aspect matters.** These sit in roughly 3:1 frames. A square or 4:3 source gets
cropped to a magnified detail and looks broken — author at 2400 × 1000.

Prompts (append the house style to each):

1. **Software & Development** — Abstract wide composition of layered translucent
   interface planes floating in dark space, seen at a shallow angle, connected
   by glowing blue circuit traces. Depth of field, the nearest plane sharp.
2. **AI & Intelligence** — A softly glowing gold neural lattice resolving out of
   darkness on the right, with blue signal traces feeding into it from the left.
   Suggests listening and answering, not a brain.
3. **Automation & Workflows** — Interlocking orthogonal conveyor paths of light,
   packets of gold energy travelling along blue rails, junctions where paths
   merge. Machinery without machines.
4. **Design & Branding** — Champagne gold geometric forms — circle, angled
   parallelogram, arc — arranged as a composition study on dark navy, with fine
   measurement lines and registration marks. Editorial, printerly.
5. **Digital Products** — An exploded stack of translucent product surfaces
   separating in space, brushed-steel edges catching light, thin blue tethers
   between the layers.
6. **Cloud & Infrastructure** — Vast dark server topology seen from above as a
   field of small blue nodes, gold trunk routes running across it, atmospheric
   haze and falloff toward the edges.
7. **Data & Analytics** — Abstract 3D data terrain: a wireframe surface of steel
   and blue with gold peaks, ridges implying trend lines. No axes, no numbers.
8. **Digital Experiences** — Long-exposure light ribbons in blue and gold curving
   through dark space, motion-blurred, one ribbon in sharp focus. Kinetic and
   immersive.

---

## 2. Project artwork — 4 images · 2400 × 1000 · JPG

Work rail slides and `/work/[slug]` headers.

| Path | Project |
| --- | --- |
| `public/media/work-voice-reception.jpg` | Vox — AI calling agent |
| `public/media/work-whatsapp-desk.jpg` | Relay — WhatsApp automation |
| `public/media/work-helios-dashboard.jpg` | Helios — analytics dashboard |
| `public/media/work-aria-agent.jpg` | Aria — knowledge agent |

**Strongly prefer real screenshots here.** A case study lives on evidence — an
actual UI shot beats abstract art every time, and visitors can tell. Use the
prompts only until the real thing exists.

1. **Vox** — Abstract audio waveform of gold light travelling along a blue
   circuit path toward a glowing ring, dark navy ground, sense of a call
   connecting.
2. **Relay** — Message-bubble silhouettes as thin blue outlines flowing along
   converging gold rails into a single bright junction point.
3. **Helios** — Dark dashboard surfaces at an angle, glowing blue chart forms and
   one gold highlighted metric, heavy depth of field. No legible text.
4. **Aria** — Layered translucent document planes with gold underline marks,
   blue retrieval lines connecting a highlighted passage to a glowing answer node.

---

## 3. Page artwork — 2 images · 2400 × 1000 · JPG

| Path | Used on |
| --- | --- |
| `public/media/about-studio.jpg` | `/about` header |
| `public/media/contact-signal.jpg` | spare — wire into `/contact` if wanted |

- **about-studio** — If you have one, a real photograph of the workspace, colour
  graded cool and dark, is far better than generated art. Otherwise: a dark
  desk surface lit from one side, screens glowing blue out of focus, gold
  circuit traces suggested in the reflection.
- **contact-signal** — A single gold signal pulse travelling out from a bright
  origin point across a dark blue circuit field, radiating rings.

---

## 3b. Menu preview cards — 7 images · 1200 × 1500 · JPG · optional

The cards that follow your cursor over the overlay menu and the services index.
They currently reuse the service and project artwork above, which works — this
is only worth doing if you want each route to have its own image.

**These are portrait, not wide.** The card is taller than it is wide, so a
landscape source gets cropped to a narrow middle strip. Author at 1200 × 1500.

Save as `public/media/menu-<name>.jpg`, then set `image` on the matching entry
in `previews` inside
[`components/layout/MenuOverlay.tsx`](../components/layout/MenuOverlay.tsx).

| File | Card |
| --- | --- |
| `menu-home.jpg` | Home — "Everything digital" |
| `menu-services.jpg` | Services — "Eight pillars, one studio" |
| `menu-work.jpg` | Work — "Selected work" |
| `menu-process.jpg` | Process — "Six steps, no mystery" |
| `menu-packages.jpg` | Packages — "Transparent pricing" |
| `menu-about.jpg` | About — "Who you'd work with" |
| `menu-contact.jpg` | Contact — "Start a project" |

Append the house style to each. Keep the **lower third quiet** — a label sits
there, over a dark scrim.

1. **Home** — Vertical composition of blue and gold circuit traces converging
   upward into a single bright point, dark navy ground, sense of many things
   becoming one.
2. **Services** — Eight softly glowing panels stacked in vertical perspective,
   each a slightly different depth, connected by thin blue traces.
3. **Work** — A tall stack of translucent interface layers seen at an angle,
   brushed-steel edges catching light, one layer highlighted in gold.
4. **Process** — Six illuminated nodes descending a single vertical blue
   circuit line, each node brighter than the last.
5. **Packages** — Three vertical champagne-gold columns of differing heights on
   dark navy, fine measurement lines beside them, editorial and precise.
6. **About** — A dark studio desk shot from above in portrait crop, screens
   glowing blue out of focus. A real photograph of your own workspace, colour
   graded cool, would be better than anything generated.
7. **Contact** — A single gold signal pulse rising from the bottom of the frame
   through a dark blue circuit field, radiating rings as it goes.

---

## 4. Video — optional

Any image on the site can have a short looping video play on top of it. This is
optional. Skip the whole section if you only want still images.

### How to add one

**Step 1.** Pick an image you want to make move. Say this one:

```
public/media/work-voice-reception.jpg
```

**Step 2.** Make a video for it, using the specs below.

**Step 3.** Name the video **exactly the same as the image**, but ending in
`.mp4` instead of `.jpg`:

```
work-voice-reception.jpg   <- the image that is already there
work-voice-reception.mp4   <- your new video, same name
```

**Step 4.** Put the `.mp4` in the same folder, `public/media`.

**Step 5.** Restart the site:

```bash
npm run dev
```

That is all. The video is found automatically because of its name. You do not
edit any code.

### What happens on the page

- The image shows first, immediately. The video fades in once it has loaded.
- If the video is slow or fails, the image just stays. Nothing breaks.
- The video is silent and repeats forever.
- It stops playing when you scroll past it, and does not play at all for
  visitors who have asked their device to reduce animation.

### Specs to give whoever makes the video

| | |
| --- | --- |
| Format | MP4 |
| Size | 2400 x 1000 pixels |
| Length | 4 to 8 seconds |
| Sound | None. Remove the audio track entirely |
| File size | Under 3 MB |
| Looping | The last frame should match the first, so the repeat is invisible |

That last one matters most. A video that ends somewhere different from where it
started will visibly jump every few seconds.

### What to make a video of

For the four project images, a **screen recording of the actual product** is far
better than anything generated. Crop it to the interesting part, and do not
include the mouse cursor or browser chrome.

If you want a generated one instead, use this prompt:

> Slow drift across a dark circuit-board surface, pulses of blue and gold light
> travelling along the traces, shallow depth of field, seamless loop, no cuts,
> no text.

### To remove a video later

Delete the `.mp4` and restart. The image takes over again on its own.

## Importing artwork you have generated

Image generators hand you PNGs. PNG is the wrong format for this kind of
artwork — it stores every pixel losslessly, so one frame is about 2 MB where the
same picture as a JPEG is nearer 200 KB and looks identical. Fourteen of them is
the difference between a 27 MB page and a 3 MB one.

**Name each file after the row it replaces in the tables above**, drop them all
in one folder, then run:

```bash
python3 scripts/import-media.py
```

It reads `~/Downloads` by default, converts every name it recognises, and leaves
your originals untouched. Pass a folder to read from somewhere else:

```bash
python3 scripts/import-media.py ~/Desktop/artwork
```

Files whose names it does not recognise are listed and skipped rather than
guessed at, and it tells you which images are still using placeholders.

### After importing, restart properly

```bash
rm -rf .next/cache
npm run dev
```

Next.js caches resized copies of every image. Replacing a file does not clear
that cache, so without this step you will keep seeing the old picture and think
the import failed. **Your browser caches them too** — use a hard refresh
(**Cmd-Shift-R**) the first time.

---

## 5. Brand assets — status

| Path | Size | Status |
| --- | --- | --- |
| `public/brand/zeizzlabs-mark.png` | 1024² transparent | ✅ installed |
| `public/brand/zeizzlabs-wordmark.png` | 1200 × 349 transparent | ✅ installed |
| `public/brand/og-image.jpg` | 1200 × 630 | ✅ generated — `scripts/generate-og.py` |
| `app/icon.png`, `app/apple-icon.png` | 512², 180² | ✅ generated |
| `public/brand/zeizzlabs-logo.png` | 1536 × 1024 | ⚠️ **off-brand** |
| `public/brand/zeizzlabs-mark-simple.png` | 512² transparent | ❌ **worth adding** |
| `public/brand/zeizzlabs-mark-alt.png` | — | duplicate, safe to delete |

**`zeizzlabs-logo.png` is the old neon purple/orange board.** It no longer
matches the navy/blue/gold brand and is no longer used anywhere on the site.
Either regenerate it in the current palette or delete it.

**A simplified mark is the one real gap.** The detailed monogram turns to mush
below about 40px — in the navbar, and worst in a browser tab. Prompt:

> A minimal single-letter Z monogram derived from a detailed circuit-board Z.
> Keep only the bold outer Z silhouette with one angled diagonal cut and at most
> three thick circuit traces. Electric blue with a single champagne gold trace.
> Flat, no bevel, no 3D, transparent background, readable at 32 pixels. Vector,
> centred, square canvas.

Save it as `zeizzlabs-mark-simple.png`, then in
`components/brand/Logo.tsx` point `MARK_SRC` at it for UI sizes.

---

## After adding files

```bash
npm run build
```

Regenerating the placeholders at any time:

```bash
python3 scripts/generate-art.py
python3 scripts/generate-og.py
```

Both overwrite in place, so run them only if you want the generated art back.
